import { TotalMarkPipe } from './total-mark.pipe';

describe('TotalMarkPipe', () => {
  it('create an instance', () => {
    const pipe = new TotalMarkPipe();
    expect(pipe).toBeTruthy();
  });
});
